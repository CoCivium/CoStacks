# CoStacks DO Block Transport Rule

DO blocks must end with real trailing newline characters.

Reason:
- PS7 paste execution requires final newline to trigger execution.
- Without trailing newline, operator must press Enter manually.

Standard ending:
}
}      # ⊂

(two blank lines after this line)
