# Seed CoTerm Cluster v0.1
UTC_CREATED: 20260306T201237Z

## Proposed terms from this session cluster
- CoExternalize = convert transient session cognition into durable shared artifacts
- CoMaterialize = turn abstract discussion into structured artifacts
- CoBusify = publish discoverable pointers onto the CoBus
- CoCarry = latent knowledge not yet externalized
- CoDrain = extraction of all actionable CoCarry before session closure
- CoArtifactDoctrine = canonical knowledge should exist as artifacts rather than chat memory
- CoDiscoverySurface = any location where orchestrators can discover artifacts
- CoFriction = infrastructure resistance encountered during automation
- CoSemanticKernel = canonical machine-native meaning layer beneath CoGibber renderings
- CoRenderShell = human-facing projection of a semantic kernel
- CoSemanticVersion = explicit semantic versioning for machine meaning
- CoEvolutionProtocol = rules allowing safe semantic evolution across versions
- CoIntentPacket = atomic communication object in a concept-first substrate
- CoMeaningGraph = graph of concept relationships between CoTerms
- CoCanonize = promote transient cognition into durable canonical artifacts

## Note
- These are candidate terms, not yet approved canonical GIBindex entries.

